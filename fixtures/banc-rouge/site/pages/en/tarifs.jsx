// Route `/en/tarifs` du site versionne : la page EXISTE dans la source, et le menu anglais la
// promet. Le menu SERVI ne la porte pas — c est le defaut H-20.
export default function Pricing() {
  return <main>Pricing</main>;
}
